include "std/std.yk";

fn main() int64 {
    let s: string = "HELLO\n";
    let i: int64 = 0;
    let n: int64 = strlen(s);
    while i < n {
        putc(nth(s, i));
        i = i + 1;
    }
    ret 0;
}
